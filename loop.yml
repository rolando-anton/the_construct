- hosts:
    all
  gather_facts: no
  roles:
    - role: bootstrap
    - role: disable-windowsdefender
    - role: domain-member
    - role: sysmon
#    - role: fortisiem-agent
  loop: "{{ query('inventory_hostnames', 'endpoints') }}"
